<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Document</title>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
    <a href="/logout" class="bg-red-400 rounded-lg px-3 py-2 text-sm mb-5 inline-block">Logout</a>
  </div>
      <h5 class="mb-5">Edit your Post</h5>
      <form class="mb-10" action="/update/<%= post._id %>" method="post">
        <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-lg p-3 outline-none" placeholder="What's on your mind ?" name="content"><%= post.content %></textarea>
        <input type="submit" class="w-40 px-3 py-2 bg-yellow-500 text-black block rounded-lg mt-2 text-sm" value="Update Post">
      </form>
  </div>
</body>
</html>