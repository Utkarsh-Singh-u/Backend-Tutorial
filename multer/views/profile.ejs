<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Document</title>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div class="w-full flex justify-end">
    <a href="/logout" class="bg-red-400 rounded-lg px-3 py-2 text-sm mb-5 inline-block">Logout</a>
  </div>
  <div class="flex item-start gap-3">
  <div class="w-10 h-10 rounded-lg overflow-hidden">
    <img class="w-full h-full object-cover" src="/images/upload/<%= user.profilepic %>" alt="">
  </div>
    <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.name %> 👋</h3>
  </div>
      <h5 class="mb-5 mt-3">Create new Post</h5>
      <form class="mb-10" action="/post" method="post">
        <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-lg p-3 outline-none" placeholder="What's on your mind ?" name="content"></textarea>
        <input type="submit" class="w-40 px-3 py-2 bg-blue-500 block rounded-lg mt-2 text-sm" value="Create New Post">
      </form>
      <hr>
      <div class="posts mt-10">
        <h3 class="text-zinc-400">Your Posts</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach(function(post){ %>
          <div class="post w-1/3 mb-4 p-4 rounded-lg border-zinc-600 border-[1px] bg-zinc-800">
            <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>
            <small class="mt-2 inline-block"><%= post.likes.length %> Like</small>
            <div class="btns flex mt-5 gap-4">
              <a class="text-blue-500" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id)=== -1 ? "Like":"Unlike" %></a>
              <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
  </div>
</body>
</html>